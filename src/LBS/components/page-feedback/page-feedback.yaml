params:
  - name: href
    type: string
    required: false
    description: If `href` is set, it will become value in `action` attribute


examples:
  - name: default
    description: Standard application of the page feedback.
    data:

  # Hidden examples are not shown in the review app, but are used for tests and HTML fixtures
  - name: classes
    hidden: true
    data:
      href: '/'
