params:
  - name: href
    type: string
    required: false
    description: If `href` is set, it will become value in `action` attribute
  - name: placeholderText
    type: string
    required: false
    description: If `placeholderText` is set, it will become value in `placeholder` attribute

examples:
  - name: default
    description: Standard application of the search bar.
    data:

  - name: with placeholder
    description: Full width variant of the search bar with custom placeholder text.
    data:
      fullWidth: true
      placeholderText: Search for advice, information and services


  # Hidden examples are not shown in the review app, but are used for tests and HTML fixtures
  - name: classes
    hidden: true
    data:
      href: '/'
